@model IList<ProductOverviewModel>
@{
    Layout = "_ColumnsTwo";

    //title
    Html.AddTitleParts(T("PageTitle.NewProducts").Text);
    //page class
    Html.AppendPageCssClassParts("html-recently-added-products-page");
}
<div class="page recently-added-products-page from-panel">
    <div class="page-title panel-heading">
        <a href="@Url.RouteUrl("NewProductsRSS")" class="link-rss pull-right" title="@T("Products.NewProducts.RSS.Hint")"><i class="fa fa-rss-square">@*@T("Products.NewProducts.RSS")*@</i></a>
        <h1>@T("Products.NewProducts")</h1>
    </div>
    <div class="page-body panel-body">
        @if (Model.Count > 0)
        {
            foreach (var product in Model)
            {
                <div class="item-box product-grid col-xs-12 col-sm-4 col-md-4 product-align clearfix">
                    <div class="thumbnail">
                        @await Html.PartialAsync("_ProductBox", product)
                    </div>
                </div>
            }
        }
    </div>
</div>
@*@if (Model.Count > 0)
        {
        <div class="page recently-added-products-page from-panel">
            <div class="page-title panel-heading">
                <a href="@Url.RouteUrl("NewProductsRSS")" class="link-rss pull-right" title="@T("Products.NewProducts.RSS.Hint")"><i class="fa fa-rss-square"></i>@T("Products.NewProducts.RSS")</a>
                <h1>@T("Products.NewProducts")</h1>
                <span class="pull-right">
                    <a class="title-carousel-control" href="#carousel-example-generic-homepage-product" data-slide="prev">
                        <i class="fa fa-angle-left"></i>
                    </a>
                    <a class="title-carousel-control" href="#carousel-example-generic-homepage-product" data-slide="next">
                        <i class="fa fa-angle-right"></i>
                    </a>
                </span>
            </div>
            <div class="page-body panel-body">
              <div id="carousel-example-generic-homepage-product" class="carousel slide" data-ride="carousel">
                <div class="carousel-inner">
                    @{
                      var ci = 0;
                    }
                    @foreach (var item in Model)
                    {
                        if (ci == 0)
                        {
                            @:<div class="item active">
                            @:<div class="row box-products">
                        }
                        else if (ci % 4 == 0)
                        {
                            @:<div class="item">
                            @:<div class="row box-products">
                         }
                        <div class="col-md-3 col-sm-3 col-xs-6">
                            <div class="offer offer-default silde-show-offer pull-text-center">
                                @await Html.PartialAsync("_ProductBox", product)
                            </div>
                        </div>
                        if (ci % 4 == 3)
                        {
                            @:</div>
                            @:</div>
                        }
                        ci++;
                    }
                    @if (ci % 4 == 3)
                    {
                        @:</div>
                        @:</div>
                      }
                </div>
              </div>
            </div>
        </div>
    }*@