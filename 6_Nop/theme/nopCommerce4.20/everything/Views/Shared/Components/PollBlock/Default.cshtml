@model PollModel
<div class="block block-poll card">
    <div class="card-header title">
        <h4 class="card-title">@T("Polls.Title")</h4>
    </div>
    <div class="listbox">
        @await Html.PartialAsync("_Poll", Model)
    </div>
</div>
