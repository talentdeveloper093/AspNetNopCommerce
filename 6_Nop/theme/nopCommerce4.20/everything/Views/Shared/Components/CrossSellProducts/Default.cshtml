@model IList<ProductOverviewModel>
@if (Model.Count > 0)
{
    <div class="product-grid cross-sells">
        <div class="title">
            <strong>@T("ShoppingCart.CrossSells")</strong>
        </div>
        <div class="row">
            <div class="item-grid">
                @foreach (var product in Model)
                {
                    <div class="item-box col-xs-12 col-sm-6 col-md-4 col-lg-3 col-xl-3">
                        <div class="card">
                            <div class="card-body">
                                @await Html.PartialAsync("_ProductBox", product)
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
}